{
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && npx gh-pages -d dist",
    "deploy:ci": "npm run build && npx gh-pages -d dist -t true"
  },
  "devDependencies": {
    "gh-pages": "^6.1.0"
  }
}
